sudo: required
language: generic 

services:
 - docker

before_install:
  - docker build -t victorfgs/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true victorfgs/docker-react npm run test